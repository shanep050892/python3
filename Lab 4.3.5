# Your task is to write and test a function which takes two arguments
# (a year and a month) and returns the number of days for the given
# year-month pair.

# Imports calendar module into Python.

import calendar

# Defining the function to be used in the code to allow the programme to calculate days in the month.

def days_in_month(year, month):
    try:
        days = calendar.monthrange(year, month)[1]
        return days
    except ValueError:
        return "Invalid input for year or month."

# Provides the user with an input to enter a month/year of their choice.

year = int(input("Enter a year: "))

month = int(input("Enter a month (1-12): "))

# This code uses isinstance to return True if the specified object id of the specified type. 

if 1 <= month <= 12:
    result = days_in_month(year, month)
    if isinstance(result, int):
        print(f"Number of days in {calendar.month_name[month]}, {year}:{result}")

    else:
        print(result)

else:
    print("Invalid month. Please enter a month between 1 and 12.")
